# พรอมต์: Goal & To-Do Life Tracker Web App

สร้าง Web App สำหรับบันทึกเป้าหมายและ To-Do List ส่วนตัว ด้วย React + Tailwind CSS (single page app)

## ภาพรวมแอพ
แอพช่วยให้ผู้ใช้ตั้งเป้าหมายชีวิต แบ่งเป็นงานย่อย/กิจวัตรประจำวัน ติ๊กทำเครื่องหมายว่าทำแล้วในแต่ละวัน และดูกราฟสรุปความคืบหน้า พร้อมมี AI ช่วยสัมภาษณ์และแตกเป้าหมายใหญ่เป็น task ย่อยให้

## โครงสร้างข้อมูล
- Goal (เป้าหมาย): มี id, ชื่อ, คำอธิบาย, ประเภท ("long-term" หรือ "habit"), วันที่สร้าง, วันที่เป้าหมาย (ถ้ามี), สี/ไอคอนประจำเป้าหมาย
- สำหรับเป้าหมายแบบ Long-term: มีรายการ Task ย่อย (id, ชื่อ, สถานะ done/not done, วันที่แนะนำให้ทำ) ที่ผูกกับ Goal นั้น ความคืบหน้า = (task ที่เสร็จ / task ทั้งหมด) x 100%
- สำหรับเป้าหมายแบบ Habit: มีการบันทึกรายวันว่าวันไหนทำสำเร็จบ้าง (เก็บเป็น array ของวันที่ เช่น ["2026-09-01", "2026-09-02"])

## ฟีเจอร์หลัก

### 1. หน้า Dashboard (หน้าแรก)
- แสดงเป้าหมายทั้งหมดเป็นการ์ด แยกกลุ่ม "เป้าหมายระยะยาว" และ "กิจวัตรประจำวัน"
- แต่ละการ์ดโชว์ % ความคืบหน้า (progress bar) และ streak ปัจจุบัน (สำหรับ habit)
- มีกราฟสรุปรวม 2 แบบ:
  a) กราฟแท่ง/วงกลม แสดง % ความคืบหน้าของแต่ละเป้าหมายระยะยาว
  b) กราฟเส้น/heatmap แบบปฏิทิน แสดงความสม่ำเสมอ (streak) ของ habit ย้อนหลัง 30 วัน

### 2. หน้าเพิ่ม/จัดการเป้าหมาย
- ฟอร์มสร้างเป้าหมายใหม่ เลือกประเภท (ระยะยาว หรือ กิจวัตรรายวัน)
- ถ้าเป็นระยะยาว: เพิ่ม/ลบ/แก้ไข task ย่อยได้เอง หรือกดปุ่ม "สร้างเป้าหมายด้วย AI" เพื่อให้ AI ช่วยแตก task ย่อยให้ (ดูรายละเอียดในหัวข้อ AI Interview ด้านล่าง)
- แก้ไข/ลบเป้าหมายเดิมได้

### 3. หน้า Check-in รายวัน
- แสดงรายการที่ต้องทำวันนี้ (habit ที่ยังไม่ติ๊ก + task ย่อยที่ค้างอยู่)
- กดติ๊กเพื่อบันทึกว่าทำแล้ว พร้อม animation ตอนติ๊กเสร็จให้รู้สึกดี (เช่น confetti เล็กๆ หรือ checkmark เด้ง)

### 4. หน้าประวัติ/สถิติ
- ปฏิทินย้อนหลังดูว่าวันไหนทำอะไรสำเร็จบ้าง
- สรุปสถิติ: streak สูงสุด, จำนวนวันที่ทำสำเร็จรวม, เป้าหมายที่ใกล้สำเร็จที่สุด

## ฟีเจอร์ AI: สัมภาษณ์และแตกเป้าหมายเป็น Task ย่อย

### Flow การทำงาน
1. เมื่อผู้ใช้กด "สร้างเป้าหมายด้วย AI" ให้เปิดหน้าต่างสัมภาษณ์แบบ **chat แบบถาม-ตอบทีละคำถาม** (conversational, multi-turn) เหมือนคุยกับผู้ช่วยจริงๆ ไม่ใช่ฟอร์มถามพรวดเดียวหมด
2. AI ต้องถามคำถามเชิงลึกทีละข้อ เพื่อทำความเข้าใจเป้าหมายผู้ใช้ให้ครบถ้วน เช่น:
   - เป้าหมายนี้คืออะไร ทำไมถึงอยากทำ (motivation)
   - มีเดดไลน์หรือกรอบเวลาไหม
   - ระดับความพร้อม/ทรัพยากรที่มีตอนนี้ (เวลาว่าง, ทักษะ, งบประมาณ)
   - เคยพยายามทำเป้าหมายนี้มาก่อนไหม ติดปัญหาอะไร
   - ชอบทำงานแบบไหน (ทีละนิดทุกวัน หรือโหมทำทีเดียว)
3. หลังถามครบ (ประมาณ 4-6 คำถาม) ให้ AI สรุปและถามยืนยันก่อนว่าเข้าใจถูกไหม
4. เมื่อผู้ใช้ยืนยัน ให้ AI แตกเป้าหมายออกเป็น task ย่อยที่เป็นรูปธรรม ทำได้จริง เรียงลำดับความสำคัญ/เวลาที่เหมาะสม พร้อมกำหนดว่า task ไหนควรทำเมื่อไหร่
5. แสดงผล task ที่ AI แนะนำให้ผู้ใช้ preview ก่อน สามารถแก้ไข/ลบ/เพิ่มเองได้ก่อนกดบันทึกเข้าเป้าหมายจริง

### การจัดการบทสนทนาแบบ Multi-turn
- Frontend ต้องเก็บ state ของบทสนทนาทั้งหมดไว้ (array ของ {role: "user"/"model", text: "..."})
- ทุกครั้งที่ส่ง request ไปที่ backend → Gemini ต้องแนบ "ประวัติสนทนาทั้งหมด" ไปในฟิลด์ contents ไม่ใช่แค่ข้อความล่าสุด เพราะ Gemini ไม่มีความจำข้าม request
- UI แสดงผลเป็นกล่องแชท (chat bubble) เหมือนแอพแชททั่วไป ข้อความ AI อยู่ซ้าย ข้อความผู้ใช้อยู่ขวา พร้อม typing indicator ระหว่างรอ AI ตอบ
- เมื่อ AI คิดว่าถามครบแล้ว ให้ขึ้นข้อความสรุปพร้อมปุ่ม "ยืนยันและแตก Task" แทนที่จะถามคำถามต่อไปเรื่อยๆ

### Technical Implementation (Gemini API)
- ใช้ Gemini API model: **gemini-3.6-flash** (รุ่นล่าสุด GA ปี 2026 รองรับ free tier ผ่าน Google AI Studio)
- Endpoint: `https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:generateContent`
- **สำคัญมาก: ห้ามเรียก Gemini API ตรงจาก frontend เด็ดขาด** เพราะจะทำให้ API key รั่วไหล ให้สร้าง backend endpoint (เช่น Next.js API route หรือ serverless function) เป็นตัวกลาง โดย frontend ยิง request มาที่ backend ของเราเอง แล้ว backend ค่อยแนบ API key ยิงต่อไปที่ Gemini ผ่าน header `x-goog-api-key`
- เก็บ Gemini API key ไว้ใน environment variable (.env) ฝั่ง server เท่านั้น ห้าม hardcode หรือ commit ขึ้น git
- ใช้ `system_instruction` กำหนดบทบาท AI ว่าเป็น "โค้ชวางแผนเป้าหมายที่ถามคำถามลึกซึ้งทีละข้อ แล้วสรุปแตกเป็น task ย่อยที่ทำได้จริง"
- ให้ AI ตอบกลับเป็น JSON structured format ตอนขั้นตอนแตก task (เช่น `{"tasks": [{"title": "...", "suggested_date": "..."}]}`) เพื่อ parse เข้าแอพง่าย ส่วนตอนสัมภาษณ์ให้ตอบเป็นข้อความปกติ
- ใส่ error handling กรณี API key ไม่ถูกต้องหรือ rate limit เต็ม (free tier มีโควต้าจำกัดต่อวันและต่อโปรเจกต์) ให้แจ้งผู้ใช้อย่างเป็นมิตร พร้อมอนุญาตให้เพิ่ม task เองแบบ manual ได้เสมอเป็น fallback
- มีช่องให้ผู้ใช้กรอก Gemini API key ของตัวเองในหน้า Settings เก็บไว้ผ่าน environment variable ตอน deploy (ไม่ hardcode ในแอพ)

## การเก็บข้อมูล: Firebase (Firestore + Authentication)
ผู้ใช้จะเข้าแอพจากหลายเครื่อง (มือถือ, คอม, iPad) จึงต้องการให้ข้อมูล sync กันผ่าน cloud ไม่ใช่แค่เก็บในเครื่องเดียว

- ใช้ **Firebase Authentication** สำหรับ login แบบง่ายที่สุด แนะนำ "Sign in with Google" (ผู้ใช้กดปุ่มเดียว ไม่ต้องจำรหัสผ่านใหม่) เป็นตัวเลือกหลัก
- ใช้ **Cloud Firestore** เป็น database เก็บข้อมูล Goal, Task, และ check-in ทั้งหมด โดยแยกข้อมูลตาม user id (uid) ของแต่ละคนที่ login เข้ามา เพื่อไม่ให้ข้อมูลปนกัน
- โครงสร้าง Firestore แนะนำ: collection `users/{uid}/goals/{goalId}` และ subcollection ย่อยสำหรับ task/check-in ในแต่ละ goal
- ตั้งค่า Firestore Security Rules ให้ user แต่ละคนอ่าน/เขียนได้เฉพาะข้อมูลของตัวเอง (`request.auth.uid == uid`)
- ใช้ Firebase SDK แบบ real-time listener (`onSnapshot`) เพื่อให้ข้อมูลอัปเดตทันทีเมื่อมีการเปลี่ยนแปลงจากเครื่องอื่น โดยไม่ต้อง refresh หน้า
- เก็บ Firebase config (apiKey, authDomain, projectId ฯลฯ) ไว้ใน environment variable ฝั่ง frontend ได้ตามปกติ (ค่าพวกนี้ของ Firebase ไม่ใช่ความลับเหมือน API key ของ Gemini เพราะการป้องกันจริงอยู่ที่ Security Rules)
- ยังคงมีปุ่ม "Export ข้อมูล" (ดาวน์โหลดเป็นไฟล์ .json) ไว้เป็น backup เสริม เผื่อกรณีฉุกเฉิน

## ขั้นตอนที่ผู้ใช้ต้องทำเอง (Manual Setup) — สำคัญมาก
บางขั้นตอนต้องทำผ่านหน้าเว็บ Firebase Console หรือ Google AI Studio ด้วยมือ AI เขียนโค้ดให้ไม่ได้ ให้ AI ที่รับพรอมต์นี้ปฏิบัติตามกฎนี้อย่างเคร่งครัด:

- เมื่อถึงขั้นตอนที่ต้องให้ผู้ใช้ไปทำเองบนเว็บ (เช่น สร้าง Firebase Project, เปิดใช้งาน Authentication, สร้าง Firestore Database, คัดลอก Firebase config, สร้าง Gemini API key, ตั้งค่า Security Rules) ให้ **หยุดและอธิบายเป็นขั้นตอนละเอียด ทีละสเต็ป** บอกชื่อปุ่ม/เมนู/ตำแหน่งที่ต้องกดให้ชัดเจน ไม่สรุปรวบรัด
- ห้ามสมมติว่าผู้ใช้รู้อยู่แล้วว่าหน้าตา UI ของ Firebase Console หรือ Google AI Studio เป็นอย่างไร ให้อธิบายราวกับผู้ใช้ไม่เคยเข้าเว็บนั้นมาก่อน
- ถ้า AI ไม่แน่ใจว่าหน้าจอปัจจุบันของผู้ใช้หน้าตาเป็นแบบไหน หรือไม่แน่ใจว่าต้องบอกให้กดปุ่มไหนต่อ (เพราะ UI ของเว็บอาจเปลี่ยนไปจากที่ AI รู้) **ให้ขอให้ผู้ใช้แคปหน้าจอ (screenshot) ส่งมาก่อนเสมอ** แล้วค่อยบอกขั้นตอนถัดไปตามภาพจริงที่เห็น อย่าเดาหรือสมมติเอาเอง
- หลังจากผู้ใช้ทำขั้นตอน manual เสร็จแต่ละครั้ง (เช่น ได้ Firebase config มาแล้ว) ให้ AI สรุปว่าต้องเอาค่าที่ได้ไปวางตรงไหนในโค้ด/ไฟล์ .env ให้ชัดเจน ก่อนไปขั้นตอนถัดไป
- แนะนำลำดับ manual setup ที่ควรทำ (AI ควรพาผู้ใช้ทำตามลำดับนี้ทีละอย่าง ไม่โยนมาให้ทำพร้อมกันหมด):
  1. สร้าง Firebase Project ใหม่ที่ Firebase Console
  2. เปิดใช้งาน Authentication และเลือก sign-in method (Google)
  3. สร้าง Cloud Firestore database (เลือกโหมดเริ่มต้นและ region)
  4. คัดลอก Firebase config มาใส่ในโปรเจกต์ (.env หรือไฟล์ config)
  5. ตั้งค่า Firestore Security Rules ตามที่ระบุไว้ข้างต้น
  6. สร้าง Gemini API key จาก Google AI Studio มาใส่ในฝั่ง backend
  7. ทดสอบ login และบันทึกข้อมูลจริงว่า sync ข้ามเครื่องได้จริง

## ดีไซน์
- โทนสีสดใส ให้ความรู้สึกสร้างแรงบันดาลใจ (เช่น โทนพาสเทล หรือ gradient อ่อนๆ)
- Mobile-first responsive design ใช้งานบนมือถือได้สะดวก
- ใช้ไอคอนจาก lucide-react ประกอบ UI ให้ดูสะอาดตา

กรุณาสร้างเป็นแอพที่ใช้งานได้จริงครบทุกฟีเจอร์ข้างต้น ไม่ใช่แค่ mockup
